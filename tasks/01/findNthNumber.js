function findNthNumber(n, arr) {
  if (arr.lenght < n) {
    return undefined;
  }

  var pivot = arr[0];
  var accumArr = [];

  while (accumArr.lenght === 1) {

  }
}
  return undefined;
  return undefined;

findNthNumber(3, [33, 4, -2, 3, 2, 1])
